<% if notice %>
  <div class="notification notice_success">
    <%= notice %>
  </div>
<% end %>

<center><h1 class="title">Room Types</h1></center>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Hour price</th>
      <th>Date price</th>
      <th>Past night price</th>
      <th>Week price</th>
      <th>Month price</th>
      <th>Add adult price</th>
      <th>Add child price</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @room_types.each do |room_type| %>
      <tr>
        <td><%= room_type.name %></td>
        <td><%= room_type.hour_price %></td>
        <td><%= room_type.date_price %></td>
        <td><%= room_type.past_night_price %></td>
        <td><%= room_type.week_price %></td>
        <td><%= room_type.month_price %></td>
        <td><%= room_type.add_adult_price %></td>
        <td><%= room_type.add_child_price %></td>
        <td><%= room_type.description %></td>
        <td><%= link_to 'Show', room_type %></td>
        <td><%= link_to 'Edit', edit_room_type_path(room_type) %></td>
        <td><%= link_to 'Destroy', room_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<!-- <%= link_to 'New Room Type', new_room_type_path %> -->

<div class="roomtype_sum_container tRow">
    <div>
        Danh sach loai phong ( <%= @room_types.ids.count %> )
    </div>
    <div>
        <%= link_to new_room_type_path do%><i class="fas fa-plus-circle"></i><% end %>
    </div>
</div>
<center>
<div class="roomtype_list_container tColumn">
    <% @room_types.each do |room_type| %>
    <div class="item tRow">
        <div class="section tRow">
            <div class="">
                <%= link_to room_type, method: :delete, data: { confirm: 'Are you sure?' } do %><i class="fas fa-minus-circle"></i><% end %>
            </div>
            <div class="tColumn">
                <div>
                    <%= room_type.name %>
                </div>
                <div>
                    VND <%= room_type.hour_price %>
                </div>
            </div>
        </div>
        <div class="section tRow">
            <div class="icon_costom">
              <%= link_to  room_type do %> <i class="fa fa-eye" style="font-size:2rem"></i><% end %>
            </div>
            <div class="icon_costom">
                <%= link_to edit_room_type_path(room_type) do%><i class="fas fa-pencil-alt"></i><% end %>
            </div>
            <div class="icon_costom">
                <i class="fas fa-dollar-sign"></i></div>
        </div>
    </div>
    <% end %>
</div>
</center>
<br>

